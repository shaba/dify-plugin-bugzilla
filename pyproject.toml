[project]
name = "dify-plugin-bugzilla"
version = "0.1.1"
description = "Read bugs, statuses and comments from any Bugzilla instance"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.32",]

[dependency-groups]
dev = []
